<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:orientation="vertical"
        android:layout_marginLeft="24dp"
        android:layout_marginRight="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <ru.simdev.evo.components.view.TextViewMontserrat
            android:id="@+id/doorphone_address"
            android:gravity="left"
            android:layout_marginBottom="6dp"
            android:textSize="@dimen/ec_basetext"
            android:textColor="@color/ec_basetext"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

        <ru.simdev.evo.components.view.TextViewMontserrat
            android:id="@+id/doorphone_entrance"
            android:gravity="left"
            android:textSize="@dimen/ec_subtext"
            android:textColor="@color/ec_subtext"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </LinearLayout>

    <ImageView
        android:id="@+id/doorphone_next"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="24dp"
        android:src="@drawable/ec_arrow_right" />

</RelativeLayout>